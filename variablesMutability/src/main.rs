/*
Variables and Mutability
1. variables are inmutable by default (You can not change the value)
2. Rust is a block-scoped language
3. Variables can be declared as mutable by using the mut keyword
4. Constants are always immutable

*/
fn main() {
    println!("Hello, world!");
}
